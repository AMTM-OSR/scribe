#######################################################
# Discard all incoming log messages that are tagged
# with the 'debug' log priority level.
# Last Modified: 2026-Mar-01
#######################################################

log {
    source(src);
    filter { level(debug); };
    flags(final);
};

#eof#
